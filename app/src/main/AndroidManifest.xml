<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hk.hku.cs.msccs_hku_assignment2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".programOverviewPage" />
        <activity android:name=".coursesPage" />
        <activity android:name=".classSchedulePage" />
        <activity android:name=".compositionFeePage" />
        <activity android:name=".onlineRegistrationPage" />
        <activity android:name=".infoSessionPage"></activity>
    </application>

</manifest>